name: engg150_2425a_reqhub
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  firebase_core: ^3.4.0
  flutter:
    sdk: flutter
  flutter_form_builder: ^9.2.1
  qr_code_scanner: ^1.0.1
  quickalert: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
